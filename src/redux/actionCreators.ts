import axios from "axios";
/* import { Action, ActionCreator, Dispatch, AnyAction } from "@reduxjs/toolkit";
import { ThunkAction } from "redux-thunk";

interface IState {
  setEmployees: number[];
}

const asyncThunkAction: ActionCreator<
  ThunkAction<Promise<Action>, IState, void>
> = () => {
  return async (dispatch: Dispatch<IState>) => {
    try {
      const text = await axios.get("");
      return dispatch({
        type: "SET_TEXT",
        text,
      });
    } catch (e) {}
  };
};
 */
